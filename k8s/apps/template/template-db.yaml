apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: template-db
  namespace: dynadoc
spec:
  serviceName: template-db-svc
  replicas: 1
  selector:
    matchLabels:
      app: template-db
  template:
    metadata:
      labels:
        app: template-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: template-db-secret
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-path
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: template-db-svc
  namespace: dynadoc
spec:
  selector:
    app: template-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
