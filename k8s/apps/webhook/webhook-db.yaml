apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: webhook-db
  namespace: dynadoc
spec:
  serviceName: webhook-db-svc
  replicas: 1
  selector:
    matchLabels:
      app: webhook-db
  template:
    metadata:
      labels:
        app: webhook-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: webhook-db-secret
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-path
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: webhook-db-svc
  namespace: dynadoc
spec:
  selector:
    app: webhook-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
