apiVersion: v1
kind: Secret
metadata:
  name: introspect-secret
  namespace: dynadoc
type: Opaque
stringData:
  # Shared secret used by downstream services to authenticate introspection calls
  # to user-service. Generate with: openssl rand -hex 32
  INTROSPECT_SECRET: "REPLACE_WITH_STRONG_RANDOM_SECRET"
